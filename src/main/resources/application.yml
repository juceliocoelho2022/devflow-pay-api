spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/devflow
    username: postgres
    password: 89327934

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate